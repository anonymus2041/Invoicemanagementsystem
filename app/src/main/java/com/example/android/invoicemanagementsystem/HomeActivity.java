package com.example.android.invoicemanagementsystem;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class HomeActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_home);
//        FragmentManager manager = getSupportFragmentManager();
//        FragmentTransaction transaction = manager.beginTransaction();
//        transaction.replace(R.id.outstanding_revenue_frag, new OutstandingRevenue_fragment());
//        transaction.replace(R.id.total_expenses_frag, new totalExpenses_fragment());
////        transaction.replace(R.id.outstanding_revenue_frag, new OutstandingRevenue_fragment());
//        transaction.commit();

    }
}
